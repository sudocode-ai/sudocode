/**
 * @sudocode-ai/better-sqlite3-win32-x64
 * Prebuilt better-sqlite3 binary for Windows x64
 */

const path = require('path');

module.exports = {
  binaryPath: path.join(__dirname, 'better_sqlite3.node')
};
