/**
 * @sudocode-ai/better-sqlite3-linux-arm64
 * Prebuilt better-sqlite3 binary for Linux arm64
 */

const path = require('path');

module.exports = {
  binaryPath: path.join(__dirname, 'better_sqlite3.node')
};
