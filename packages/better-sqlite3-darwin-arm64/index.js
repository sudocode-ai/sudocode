/**
 * @sudocode-ai/better-sqlite3-darwin-arm64
 * Prebuilt better-sqlite3 binary for macOS arm64 (Apple Silicon)
 */

const path = require('path');

module.exports = {
  binaryPath: path.join(__dirname, 'better_sqlite3.node')
};
