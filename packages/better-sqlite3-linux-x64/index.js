/**
 * @sudocode-ai/better-sqlite3-linux-x64
 * Prebuilt better-sqlite3 binary for Linux x64
 */

const path = require('path');

module.exports = {
  binaryPath: path.join(__dirname, 'better_sqlite3.node')
};
