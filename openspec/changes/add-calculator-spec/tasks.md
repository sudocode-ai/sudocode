# Tasks

## Implementation Order

1. **Validate proposal**
   - Run `openspec validate add-calculator-spec --strict`

2. **Apply the change**
   - Run `openspec apply add-calculator-spec`

3. **Verify spec creation**
   - Check `openspec/specs/calculator/spec.md` exists
