# Guidelines

In this project, we use `sudocode` for issue tracking, and the /.sudocode/specs directory for specs management. If working on a sudocode issue, make sure to update its status after completing the task.

This repository uses Typescript. Use Typescript for all implementations.
